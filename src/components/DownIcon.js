import React, { Component } from 'react';

const DownIcon = props => {
    return (
        <div>
            <img className="imgdown" src={props.icon} />
        </div>
    );
};

export default DownIcon;
